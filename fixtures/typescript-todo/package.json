{
  "name": "typescript-todo",
  "version": "0.1.0",
  "scripts": {
    "test": "vitest run tests/public",
    "build": "tsc --noEmit",
    "test:hidden": "vitest run tests/hidden"
  },
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
